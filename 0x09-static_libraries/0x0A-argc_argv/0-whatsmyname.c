#include <stdio.h>
#include <stdlib.h>
/**
 * main - print name
 * @argc: count of arguments
 * @argv: array of values
 * Return: 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
