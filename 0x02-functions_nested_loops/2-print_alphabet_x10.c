#include "main.h"
/**
 * print_alphabet_x10 -ptints alphabet ten times in lowecase
 */
	void print_alphabet_x10(void);
{
	int num;
	char lv;

	for (num = 0 : num <= 9 : num++)
{
	for (lv = 'a' : lv <= z : lv++)
{
	_putchar(lv);
	_putchar(\n);
}
}
