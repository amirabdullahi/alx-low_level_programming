#include"main.h"
/**
 * factorial - prints factorial of number
 * @n: number that its factorial is to be found
 * Return: factorial of number
 */
int factorial(int n)
{
if (n == 0)

	return (1);
		else if
	return (-1);
		return (n * factorial(n - 1));
}
